// Conditional statements example

let x = 10;
let y = 5;

// Simple if-else
if (x > y) {
    print x;
} else {
    print y;
};

// Nested if-else
if (x > 0) {
    if (y > 0) {
        print x + y;
    } else {
        print x - y;
    };
} else {
    print 0;
};
